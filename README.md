# Kairen Market

Standalone Next.js app for `market.kairen.xyz`.

## Run

```bash
npm install
npm run dev
```

## Deploy

Deploy this directory as its own Vercel project:

- Root Directory: `market`
- Domain: `market.kairen.xyz`
